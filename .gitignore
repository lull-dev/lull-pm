node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Tauri
/src-tauri/target
/src-tauri/gen/schemas

# Generated mobile projects — CI runs `tauri android init` / `tauri ios init` on every build,
# so these are rebuilt from tauri.conf.json rather than tracked.
/src-tauri/gen/android
/src-tauri/gen/apple

# Local scratch vault used for development
/.devvault
